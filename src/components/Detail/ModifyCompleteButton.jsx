import styled from 'styled-components';
import {ModifyButton} from './ModifyButton';

export const ModifyCompleteButton = styled(ModifyButton)`
  /* display: ${props => (props.$isModifying ? 'inline' : 'none')}; */
`;
